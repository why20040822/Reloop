# 2026-08-14 工作日志

## Reloop 项目框架搭建
- 读取 `D:/桌面/Reloop/Reloop 产品定义.docx` 与 `Reloop技术方案.docx`，理解核心：触达优先级推荐"今天最该联系谁及为什么"。
- 在 `F:/ttc/Reloop` 搭建 Python + FastAPI + SQLAlchemy 框架（26 个源文件）。
- 核心评分算法为真实实现并数值验证通过：加权乘法模型 Score=活跃^0.3×匹配^0.4×价值^0.15×关系^0.1×倾向^0.05；活跃度用牛顿冷却定律；匹配度用余弦相似度。
- 数据隔离：所有业务表带 owner_user_id(飞书 open_id)，utils/isolation.py 统一过滤。
- 约束：仅用 RDS MySQL(无向量库)，向量以 JSON 存 MySQL、应用层算余弦。env 统一 BRAINX_ 前缀。
- 已知待补：TTC 人才库(ttcadvisory.com)同步的站点真实接口路径与字段映射；BRAINX_MYSQL_HOST 需用户填真实 RDS 地址。
- 项目 venv 在 `.venv`，已装最小依赖；dashscope/lark-oapi/oss2 为懒加载，配凭据后即接入。

## 二次重构（用户要求精简，下午）
- 移除：阿里云 OSS、外部活跃信号、飞书开放平台、定时调度、`.venv`。
- 外部接口只剩三类：TTC 私域人才库(数据源)、大模型(OpenAI 兼容通用接口, httpx 直连)、RDS MySQL。
- 目录改为模块化：`reloop/modules/{sync,profile,scoring,recommend}` + `api/` + `db/` + `schemas/` + `utils/`。
- 活跃度因子改由"TTC 平台 last_active_at + 站内 interaction_records"构成（牛顿冷却保留）。
- 新增：`tests/test_pipeline.py` 全流程测试（SQLite 覆盖 + LLM 离线降级，无凭据可跑）。
- LLM 无 Key 时 embedding 用本地确定性哈希向量兜底（256 维 bigram），余弦匹配离线可算。
- 环境改 conda：`environment.yml`（env 名 reloop, Python 3.11），不再用 venv。
- 隔离键改为通用 user_id（X-Owner-User-User-Id 头），非飞书 open_id；前端接入后只改 api/deps.py。
- TTC 页面需飞书登录（已实测），支持"页面导出 JSON 导入"方式：POST /sync/ttc/ingest。
- 引擎输出 Top3/Top10/TopN 同构结构，落库 recommendations 表（run_id 批次）。

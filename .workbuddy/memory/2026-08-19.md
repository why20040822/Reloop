# 2026-08-19 工作日志

- **梳理 Reloop 模块流程**：读完全部模块（config/sync/normalizer/profile/llm/scoring/recommend/api/db/schemas），输出 8 环链路 + 每阶段输入/输出 + 算法；澄清"飞书"在当前代码里不是被直接调用的外部接口（仅为 TTC 的登录 IdP，活跃信号模块已移除），真实外部接口仅 3 个：TTC/LLM/RDS。
- **探测云端端口**：47.110.93.137 上 Reloop 对外端口 = **80**（`/health`=`Reloop`、`/docs`=200）；8000 外网不可达；`/` 根 404（SPA 未挂载）；3100 是另一个项目。结论已写入 MEMORY.md「云端部署」。
- **部署 Reloop 到云端（成功）**：装好 `aliyun` CLI（v3.4.11，置于 /usr/local/bin 与 C:\Users\HP\bin）。本机 `~/.ssh/id_ed25519.pub` 由用户经控制台 Workbench 加入服务器 `/root/.ssh/authorized_keys`，实现 root 免密登录。磁盘曾 100% 满 → `apt-get clean`+`journalctl --vacuum-size=100M`+删旧日志腾出 root 保留块。本地 tar 打包（排除 .git/.workbuddy/.env/venv 等）→ scp 到 /tmp → 解压 → 保留服务器 `.env`、同步代码进 `/opt/reloop` → `chown reloop:reloop` → venv `pip install`(已满足) → `systemctl restart reloop`。验证：公网 `http://47.110.93.137/health` 返回 ok，`/docs`=200，服务 active。详见 MEMORY.md「云端部署」。
- **推荐算法 v2 建模（活跃度+匹配度）**：活跃度=分事件半衰期冷却(平台活跃14d/档案更新21d/面试30d/通话消息45d)+绝对/相对混合归一化(0.4·新近度+0.6·minmax)；匹配度=五维加权(title bigram-Dice 0.25 + skill 覆盖率 0.30 + semantic 余弦 0.35 + years 0.05 + edu 0.05)，缺失维度权重重归一。358 人真实数据实测 Top10 合理。本地+云端均已部署，test_pipeline 全过。
- **数据重置 + LLM 切换智谱（进行中）**：用户要求删旧数据换 TTC 最新 368 人。已删 RDS(reloop_app) 旧 719 条(talents/recommendations/interactions)。**LLM 切换智谱 BigModel**（`https://open.bigmodel.cn/api/paas/v4`，glm-4-flash + embedding-3 2048 维；glm-4.5 实测 60s 超时不可用）。服务器后台跑 /tmp/reloop_sync.py 全量同步（每人 structure+embed 约 7 人/分钟）。**关键坑**：① pydantic-settings 的 env_file=".env" 是相对路径，脚本必须 os.chdir('/opt/reloop') 否则读不到配置（表现为 token 空静默同步 0 条）；② sync_for_user 传外部 session 只 flush 不 commit，脚本必须自己 db.commit()；③ 本机公网 IP 162.210.155.12 需加入 RDS 白名单本地才能连库。旧 positions 的 256 维哈希 jd_embedding 已清（与新 2048 维不匹配）。
- **匹配算法 v3：LLM 职位语义相似度**：llm.py 新增 `title_similarity(jd_position, talent_positions)`——去重+分批(40/批)+进程内缓存；engine._rank 批量算 map 传入 match_score_v2 的 title 维度。实测"AI研发工程师"→机器学习专家 0.90/算法工程师 0.70/招聘经理 0.10，解决字面匹配失灵。离线降级 bigram。
- **README 重写**：删过时内容（合并部署变更记录、已完成的待办），写清本地三步跑通（conda + .env 智谱/RDS/TTC + uvicorn），算法 v2/v3 说明。用户明确**不推 GitHub**（接口不稳定，本地=云端一致即可）。

# 2026-08-18

## 前后端合并为单进程部署
- `reloop/main.py`：在 API 路由之后用 `app.mount("/", StaticFiles(html=True))` 伺服 `webapp/` 静态 SPA（同源免 CORS）。新增配置 `BRAINX_SERVE_WEBAPP`(默认 true) / `BRAINX_WEBAPP_DIR`(默认自动取项目根 webapp/)。
- `reloop/config.py`：加 `serve_webapp` / `webapp_dir` 字段与 `webapp_path` 属性。
- `webapp/data/provider.js`：数据层改为 `mode`(live/mock)，live 且 apiBase 留空=同源调用，无需配 CORS/地址；`isMock()`→`useMock()`。
- `webapp/app.js` + `i18n.js`：设置页改为「真实 API / 样本」切换，补充 `mode_label` 等文案。
- 验证：用 conda `reloop` 环境 + SQLite 起 uvicorn: `/`→index.html、`/docs`、`/health`、authed `/positions`·`/recommend/compute` 全通；中文参数须 URL 编码否则 h11 报 Invalid HTTP。
- `README.md` 重写：合并架构图、一条命令运行、完整接口清单(§4 共 12 个)、配置、测试、旧 `server.js` 纯前端预览说明。
- 顺手沉淀 skill: `~/.workbuddy/skills/fastapi-serve-spa`（FastAPI 伺服 SPA 的挂载顺序/SPA 回退/中文参数坑）。

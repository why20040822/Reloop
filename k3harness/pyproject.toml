[project]
name = "k3harness"
version = "0.1.0"
description = "K3 lightweight agent harness"
requires-python = ">=3.10"
dependencies = ["httpx", "pydantic", "typer", "pyyaml"]

[project.scripts]
k3h = "k3h.cli:app"
